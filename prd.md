# 桌面快速访问互动宠物（PawDesk）产品需求文档

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 项目名称 | 桌面快速访问互动宠物 |
| 项目代号 | PawDesk |
| 文档类型 | 产品需求文档（PRD） |
| 当前版本 | **v0.3** |
| 状态 | 草案（可作为 tech / design 的依据） |
| 目标平台 | Windows 10 / 11（x64 为主） |
| 开发语言约束 | Rust |
| 下游文档 | `tech.md`、`design.md`、`env.md` |

### 1.1 文档约定

- 本文档是**产品意图的唯一真相源**：改需求优先改 PRD，再同步下游文档与实现。
- PRD 描述「做什么、为谁、做到什么程度」；技术方案见 `tech.md`，视觉与交互细节见 `design.md`。
- 下游文档可以细化本 PRD，**不得与本 PRD 冲突**；冲突时以 PRD 为准。
- 纯实现选型（crate、模块拆分）或纯视觉 token（色值、圆角）不必回写 PRD。

### 1.2 版本记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v0.1 | 2026-07-31 | 初稿：核心功能、性能目标、Rust 约束（手写） |
| v0.2 | 2026-08-01 | 补全产品层结构：背景目标、用户与场景、首期/非目标、交互规则、系统能力、验收标准、约束与同步约定 |
| v0.3 | 2026-08-04 | 同步已实现宠物动作清单（待机/30s 撒娇/飞扑/边缘/拖动/提醒）；细节见 tech §6.1.1 |

---

## 2. 背景与目标

### 2.1 要解决的问题

长时间使用电脑时，用户容易久坐不动、忘记喝水或起来活动；同时日常启动常用软件需要反复找图标或任务栏，流程偏碎。现有桌宠多为纯观赏，健康提醒类应用又偏工具化、缺少情感与趣味。

### 2.2 产品目标

做一款**常驻 Windows 桌面**的轻量互动宠物应用，同时提供：

1. **情感陪伴**：动漫奶牛猫形象，待机动画与鼠标互动，降低「工具感」。
2. **健康提醒**：定时用幽默方式提醒站起来活动 / 照顾身体，并通过「投喂」完成闭环。
3. **快速访问**：点击宠物展开快捷菜单，一键启动用户添加的 Windows 软件。

### 2.3 成功标准（产品层）

- 用户愿意让宠物**常驻桌面**，不因打扰或卡顿而关闭。
- 健康提醒能在不强制打断的前提下**被注意到并完成投喂闭环**。
- 从点击宠物到启动常用软件，路径短于「桌面找图标 / 开始菜单搜索」的日常习惯。
- 资源占用可接受：典型待机 **CPU &lt; 3%**、**内存 &lt; 200MB**（测量条件见第 9 节）。

---

## 3. 目标用户与使用场景

### 3.1 目标用户

| 类型 | 描述 |
| --- | --- |
| 主力用户 | 长时间坐在 Windows 电脑前的个人用户（学习、办公、编程、轻度创作） |
| 次要用户 | 喜欢桌宠 / 二次元形象、希望桌面更有趣的用户 |

首期不做企业管控、多用户账号、云同步等能力。

### 3.2 核心场景

| ID | 场景 | 期望结果 |
| --- | --- | --- |
| S1 | 开机或启动应用后，宠物出现在桌面 | 透明悬浮、可拖动；位置与配置可恢复 |
| S2 | 用户工作中，暂不操作宠物 | 循环播放待机撒娇动画，低打扰、低占用 |
| S3 | 宠物靠近屏幕边缘 | 可部分隐藏并探头，需要时点一下恢复 |
| S4 | 鼠标靠近宠物 | 宠物观察并向鼠标方向扑近，播放互动动画 |
| S5 | 到了提醒时间 | 宠物进入提醒流程，展示幽默文案与食物按钮；投喂后返回原位 |
| S6 | 用户想快速开软件 | 单击宠物 → 径向快捷菜单 → 点选启动 |
| S7 | 管理常用软件入口 | 添加 / 删除 / 排序快捷方式，重启后保留 |
| S8 | 暂时不想被提醒或看到宠物 | 通过托盘暂停提醒、隐藏宠物或退出应用 |

---

## 4. 范围划分

### 4.1 首期范围（MVP，v0.2）

必须交付：

1. 单一宠物形象：可爱动漫风格**奶牛猫**。
2. 透明悬浮桌面显示，可自由拖动；支持置顶的轻量存在感（具体窗口策略由 tech 定）。
3. 待机：默认坐姿眨眼；约每 30s 随机播一次单次动作（伸懒腰/卖萌等），播完回眨眼。
4. 屏幕边缘：部分隐藏身体 + 探头观察。
5. 鼠标靠近：先 **观察**；飞扑扑近与落地互动 **后期专项**（当前关闭）。
6. 健康提醒：默认每隔 **1 小时**触发；完整投喂闭环（见 5.4）。
7. 单击宠物展开以宠物为中心的快捷菜单；菜单有动画与统一视觉风格。
8. 快捷方式：添加、删除、排序、点击启动 Windows 程序 / 快捷方式。
9. **系统托盘**：显示/隐藏宠物、暂停/恢复提醒、打开设置、退出应用。
10. **基础设置**：至少支持提醒开关与相关必要项、快捷方式管理入口；配置本地持久化。
11. 满足第 9 节性能参考指标。

### 4.2 非首期（明确不做，避免 scope 蔓延）

- 多宠物形象商城 / 付费形象（首期只预留扩展空间）。
- 基于电脑使用时长的智能提醒模型。
- 云同步、账号系统、跨设备同步。
- 自定义动画编辑器。
- 全局快捷键配置（可后续扩展）。
- 复杂多屏高级布局策略（首期仅需合理、可预测的基础多屏行为）。
- macOS / Linux。
- 社交、联网内容、广告。

### 4.3 后续可扩展（写入路线图，不承诺首期）

- 更多宠物形象与动作资源。
- 自定义提醒文案；智能提醒。
- 菜单内更多功能入口；全局快捷键。
- 提醒「迷你模式」（不强制移到屏幕中央）等体验增强。

---

## 5. 功能需求

### 5.1 宠物形象

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| F-PET-01 | 首期形象为可爱动漫奶牛猫 | P0 |
| F-PET-02 | 形象与动画资源可替换扩展，为后期新形象预留结构（不要求首期 UI 选形象） | P2 |

### 5.2 桌面显示与拖动

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| F-UI-01 | 宠物以透明悬浮窗形式显示在桌面 | P0 |
| F-UI-02 | 用户可拖动宠物到任意合理桌面位置 | P0 |
| F-UI-03 | 重启应用后恢复上次位置与用户配置 | P0 |
| F-UI-04 | 点击宠物主体可交互；透明区域不应误挡桌面操作（命中区域策略由 design/tech 细化） | P0 |

### 5.3 动画与互动

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| F-AN-01 | 日常待机为坐姿呼吸/眨眼循环；约每 30s 随机一次单次撒娇动作（伸懒腰/卖萌/轻摇/小睡），结束后回待机 | P0 |
| F-AN-02 | 动画集合可扩展，便于后续加动作或替换资源 | P1 |
| F-AN-03 | 靠近屏幕边缘时，可隐藏部分身体并探头观察 | P0 |
| F-AN-04 | 检测鼠标距离；中距/近距先 **观察**；**飞扑扑近延后**（后期专项） | P0 / 飞扑延后 |
| F-AN-05 | 扑近后播放互动动画，结束后回到待机（依赖 F-AN-04 飞扑，**后期**） | P1 延后 |

**已实现动作一览（产品层，2026-08-04）** — 实现/资源细节以 `tech.md` §6.1.1 为准：

| 场景 | 用户可见行为 | 状态 |
| --- | --- | --- |
| 日常待机 | 坐姿呼吸 + 偶发眨眼 | 已实现 |
| 定时撒娇 | 约 30s 墙钟随机：伸懒腰 / 卖萌 / 轻摇 / 小睡（≥约 3s 可见），中距观察不打断计时 | 已实现 |
| 鼠标中距 / 近距 | 观察（`Watching`）；**飞扑扑近已关闭，后期实现** | 观察已实现 / 飞扑延后 |
| 屏幕边缘 | 部分隐藏 + 探头；点击/靠近恢复 | 已实现 |
| 拖动 | 拎起晃动反馈；松手回待机 | 已实现 |
| 健康提醒 | 移向中央 + 提醒姿态 → 投喂姿态 → 回位待机 | 已实现 |
| 快捷菜单打开 | 坐姿待机（不抢菜单） | 已实现 |

### 5.4 健康提醒与投喂

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| F-RM-01 | 默认每隔 1 小时触发一次提醒（首期固定间隔可接受；设置中可预留调整） | P0 |
| F-RM-02 | 提醒流程：进入提醒状态 → 从当前位置移动到屏幕中央 → 播放提醒动画 → 显示幽默提示文字 → 出现食物按钮 → 用户点击食物完成投喂 → 宠物返回原位置 | P0 |
| F-RM-03 | 内置多条幽默提示语，随机或轮换展示 | P0 |
| F-RM-04 | 用户可通过托盘**暂停 / 恢复**提醒 | P0 |
| F-RM-05 | 提醒进行中若用户正在拖动宠物，不打断拖动；拖动结束后若提醒仍待处理，再继续提醒 | P1 |
| F-RM-06 | （后续）自定义提醒文案 | P2 |
| F-RM-07 | （后续）根据电脑使用时间智能提醒 | P2 |

**提示语示例（可直接用于首期文案池）：**

1. 喂我之前，先喂一下你的健康：站起来走两步。
2. 我已经快饿扁了，你的腰也快僵了。
3. 主人，你不是我的坐骑，请不要骑着椅子不动。
4. 再不起来，我就要怀疑你被椅子封印了。

### 5.5 软件快捷访问

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| F-SC-01 | **单击**宠物主体展开快捷菜单 | P0 |
| F-SC-02 | 菜单以宠物为中心展开，带互动感动画，风格与宠物统一 | P0 |
| F-SC-03 | 根据宠物在屏幕中的位置自动判断展开方向，避免菜单被裁切 | P0 |
| F-SC-04 | 支持添加 Windows 快捷方式 / 可执行程序入口 | P0 |
| F-SC-05 | 支持删除、调整排序；点击项直接启动对应软件 | P0 |
| F-SC-06 | 删除仅移除应用内条目，不删除用户磁盘上的原文件 / 原快捷方式 | P0 |
| F-SC-07 | 目标路径失效时保留条目并给出可理解提示，引导修复或删除 | P1 |
| F-SC-08 | 菜单结构支持后续增加更多功能入口 | P1 |
| F-SC-09 | （后续）快捷键启动 | P2 |

### 5.6 系统托盘与设置

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| F-TR-01 | 提供系统托盘图标 | P0 |
| F-TR-02 | 托盘菜单至少包含：显示/隐藏宠物、暂停/恢复提醒、打开设置、退出 | P0 |
| F-ST-01 | 提供设置界面入口，用于快捷方式管理与提醒相关设置 | P0 |
| F-ST-02 | 配置与快捷方式列表本地持久化，重启后生效 | P0 |

### 5.7 交互优先级（产品规则）

当多种行为可能冲突时，按以下产品优先级理解（实现细节由 tech 状态机落实）：

1. **用户主动拖动** 优先于提醒打断（拖完再处理待办提醒）。
2. **提醒** 优先于普通待机 / 鼠标扑近类自动互动。
3. **快捷菜单打开时** 以菜单交互为主，避免同时触发扑近等抢焦点行为。
4. 隐藏于边缘时，用户点击探头区域应能恢复，并可继续其他操作。

---

## 6. 非功能需求

### 6.1 性能

| 指标 | 目标 | 说明 |
| --- | --- | --- |
| 常驻方式 | 后台常驻桌面 | 非一次性工具 |
| CPU | 典型待机 &lt; 3% | 单机本地测量，受硬件与 GPU 后端影响 |
| 内存 | 典型运行 &lt; 200MB | 含资源加载后的稳定运行态 |
| 交互反馈 | 点击 / 拖动 / 菜单展开等应有即时反馈 | 具体动效时长由 design 定义 |

### 6.2 体验与打扰度

- 宠物应「轻量存在」：默认可爱有存在感，但不弹系统级骚扰通知刷屏。
- 提醒幽默、可完成、可暂停，避免无法关闭的强打断。
- 关闭设置窗口不得误杀宠物进程（退出仅通过托盘「退出」或等价明确操作）。

### 6.3 稳定性与数据

- 异常启动软件、失效路径、写配置失败时，应用不崩溃；给用户可读提示，详细信息可记日志。
- 配置写入应尽量安全（避免断电/强杀导致配置文件损坏；具体策略由 tech 定）。

### 6.4 隐私与安全

- 不上传用户数据；首期无账号、无云同步。
- 仅访问用户主动添加的快捷方式 / 程序路径及应用自身配置。
- 不要求管理员权限作为常规运行前提。
- 不扫描全盘、不收集键鼠内容做上传。

### 6.5 兼容性

- 目标：Windows 10 / 11，x64 为主。
- 需考虑常见 DPI / 缩放；多显示器下宠物位置不得「丢到不可见区域」且无法找回（至少可通过托盘显示或重置类能力恢复，细节由 tech/design 定）。

---

## 7. 技术约束（产品给定，非实现方案）

| 约束 | 说明 |
| --- | --- |
| 语言 | 使用 **Rust** 开发 |
| 平台 | 首期仅 Windows 桌面 |
| 形态 | 桌面悬浮宠物 + 本地配置，非 Web 套壳主路径 |
| 依赖原则 | 优先可控、可维护；保持低占用与可分发体积意识 |

具体窗口库、渲染器、托盘 crate 等由 `tech.md` 决策，不在 PRD 锁定。

---

## 8. 里程碑建议（产品视角）

| 阶段 | 目标 | 完成标志 | 状态（2026-08-04） |
| --- | --- | --- | --- |
| M1 | 桌面宠物可见可拖 | 透明窗 + 基础动画 + 拖动 | ✅ |
| M2 | 互动成型 | 边缘探头 + 鼠标扑近 | ✅（飞扑后置） |
| M3 | 提醒闭环 | 定时提醒 + 投喂 + 回位 | ✅ |
| M4 | 快捷访问 | 菜单 + 增删排序启动 + 持久化 | ✅（启动坞卡片） |
| M5 | 可日常使用 | 托盘、设置、性能与稳定性达标 | ✅ |
| **M6** | **质量迭代** | 修 bug → 交互逻辑 → UI 修复 | **进行中（下一阶段）** |
| **M6-D+** | 形象与动作精修 | 飞扑/新 clip/形象（**最后**） | 未开始 |

详细技术排期与 backlog 见 `task.md` §12–§13；设计约定见 `design.md` §1 下阶段说明。

---

## 9. 验收标准

首期可验收当且仅当以下条目基本满足：

1. 启动后奶牛猫以透明悬浮形式出现在桌面，可拖动，重启后位置/配置可恢复。
2. 待机以眨眼为主；约 30s 内会出现一次伸懒腰类动作后回到眨眼。
3. 拖到屏幕边缘可部分隐藏并探头；点击可恢复。
4. 鼠标靠近时有观察；飞扑/落地互动验收改后期专项。
5. 约 1 小时（测试可临时缩短间隔）触发提醒：移到中央、文案、食物按钮、投喂后回原位。
6. 托盘可暂停/恢复提醒、显示/隐藏宠物、打开设置、退出。
7. 单击宠物打开快捷菜单；可添加/删除/排序/启动软件；失效路径有提示。
8. 典型待机场景 CPU &lt; 3%、内存 &lt; 200MB（在约定测试环境下测量）。
9. 无账号网络依赖即可完成上述闭环。

---

## 10. 开放问题（待产品拍板）

以下不影响 v0.2 开工，但实现前建议明确：

| ID | 问题 | 建议默认 |
| --- | --- | --- |
| Q1 | 提醒间隔首期是否允许设置里修改？ | 允许，默认 60 分钟 |
| Q2 | 开机自启是否首期提供？ | 可选 P1，默认关闭 |
| Q3 | 单击展开菜单 vs 双击或其他手势？ | 首期单击展开 |
| Q4 | 提醒是否允许「稍后」而不仅是投喂完成？ | 可做 P1；至少支持暂停提醒 |
| Q5 | 多显示器宠物所在屏被拔掉时的行为？ | 移回主屏安全区 |

---

## 11. 变更流程（给后续改需求用）

1. 在本文件修改对应章节，并更新 **版本号** 与 **1.2 版本记录**。
2. 说明变更属于：范围 / 行为规则 / 非功能 / 约束。
3. 同步检查并更新 `tech.md`、`design.md`（及相关则 `env.md`）。
4. 再改代码或让 AI 按「新 PRD + 更新后的 tech/design」实现。
5. 仅视觉微调可只改 `design.md`；仅实现重构且产品行为不变可只改 `tech.md`。
