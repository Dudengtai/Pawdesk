# PawDesk 产品需求文档（PRD）

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 桌面快速访问互动宠物 |
| 代号 | PawDesk |
| 版本 | **v0.5**（2026-08-06：待机动画质量 + 宠物大小可调） |
| 平台 | Windows 10 / 11（x64） |
| 语言约束 | Rust |
| 下游文档 | `tech.md`（技术）· `design.md`（视觉与交互）· `task.md`（排期） |

**文档约定**

- PRD 是产品意图的**唯一真相源**：改需求先改本文，再同步 tech / design / 实现。
- 本文只写「做什么、为谁、做到什么程度」；**不写** crate、模块拆分、色值、算法细节。
- 冲突时以 PRD 为准。

---

## 1. 背景与目标

### 1.1 问题

长时间用电脑容易久坐、忘记活动；常用软件又要在桌面/开始菜单里反复找。纯桌宠缺工具价值，纯健康工具又缺陪伴感。

### 1.2 产品目标

常驻 Windows 桌面的**轻量互动宠物**，同时提供：

1. **陪伴**：可爱形象 + 待机与简单互动，降低工具感。  
2. **健康提醒**：定时幽默提醒起来活动，用「投喂」完成闭环。  
3. **快速访问**：点宠物打开启动坞，一键打开常用软件。

### 1.3 成功标准

- 愿意常驻，不因打扰或卡顿关掉。  
- 提醒可被注意到、可完成投喂、可暂停。  
- 开常用软件路径短于「找图标 / 搜开始菜单」。  
- 典型待机：**CPU &lt; 3%**、**内存 &lt; 200MB**。

---

## 2. 用户与场景

### 2.1 用户

| 类型 | 描述 |
| --- | --- |
| 主力 | 长时间坐在 Windows 前的个人用户（学习、办公、编程等） |
| 次要 | 喜欢桌宠 / 二次元、希望桌面更有趣的用户 |

首期不做企业管控、账号、云同步。

### 2.2 核心场景

| ID | 场景 | 期望 |
| --- | --- | --- |
| S1 | 启动应用 | 桌面出现可拖透明宠物；位置与配置可恢复 |
| S2 | 工作中不操作 | 低打扰待机动画，占用低 |
| S3 | 拖到屏幕边缘 | 可半隐藏并探头，点一下恢复 |
| S4 | 鼠标靠近 | 有观察反馈（飞扑扑近为后期） |
| S5 | 到点提醒 | 到中央展示文案与食物；投喂后回原位 |
| S6 | 想开软件 | 单击宠物 → 启动坞 → 一点即启 |
| S7 | 管理入口 | 添加 / 删除 / 排序快捷方式，重启仍在 |
| S8 | 暂时不想被打扰 | 托盘：隐藏宠物、暂停提醒、退出 |

---

## 3. 范围

### 3.1 首期要做（MVP）

1. 单一形象：动漫风格**奶牛猫**。  
2. 透明悬浮窗，可拖动，有置顶存在感。  
3. 待机眨眼；约每 **30 秒**一次随机撒娇动作。  
4. 边缘半隐藏 + 探头。  
5. 鼠标靠近：**观察**（飞扑后期）。  
6. 健康提醒：默认 **1 小时**；完整投喂闭环。  
7. 单击展开**启动坞**（宠物为锚点，位置自适应不裁切）。  
8. 快捷方式：增删排序、启动程序 / 快捷方式。  
9. 系统托盘：显示/隐藏、宠物变大/变小、暂停/恢复提醒、设置、退出。  
10. 基础设置（含宠物大小）+ 本地配置持久化。  
11. 满足第 6 节性能目标。

### 3.2 明确不做

- 多形象商城 / 付费内容  
- 基于使用时长的智能提醒模型  
- 云同步、账号、跨设备  
- 自定义动画编辑器  
- 全局快捷键配置（可后续）  
- macOS / Linux  
- 社交、联网运营、广告  

### 3.3 可扩展（不承诺首期）

更多形象与动作、自定义提醒文案、智能提醒、菜单更多入口、提醒迷你模式等。

---

## 4. 功能需求

优先级：P0 必须 · P1 应有 · P2 可后置。

### 4.1 形象与桌面

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| F-PET-01 | 首期形象为可爱动漫奶牛猫 | P0 |
| F-PET-02 | 资源结构可扩展新形象（首期可不提供选形象 UI） | P2 |
| F-UI-01 | 透明悬浮显示在桌面 | P0 |
| F-UI-02 | 可拖动到合理桌面位置 | P0 |
| F-UI-03 | 重启后恢复位置与配置 | P0 |
| F-UI-04 | 仅宠物实体可点；透明处尽量不挡桌面 | P0 |
| F-UI-05 | 宠物显示大小可调；重启后保持 | P0 |

### 4.2 动画与互动

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| F-AN-01 | 待机真眨眼（眼皮盖住虹膜，非叠层污斑）；约 30s 随机一次撒娇后回待机 | P0 |
| F-AN-02 | 动画集合可扩展 | P1 |
| F-AN-03 | 边缘半隐藏 + 探头，可恢复 | P0 |
| F-AN-04 | 鼠标靠近：观察；**飞扑后期** | P0 / 延后 |
| F-AN-05 | 扑近后互动动画（依赖飞扑，**后期**） | P1 延后 |
| F-AN-06 | clip 切换有短过渡，忌硬切；拖动时有拎起动画 | P1 |

**用户可见行为（产品层）**

| 场景 | 行为 |
| --- | --- |
| 待机 | 近静坐姿 + 自然眨眼（闭眼可读，无黑块叠在黄眼上） |
| 定时撒娇 | 约 30s：伸懒腰 / 卖萌 / 轻摇 / 小睡等，可看清后再回待机 |
| 鼠标靠近 | 观察（可朝向光标）；飞扑暂不做验收 |
| 边缘 | 半隐藏 + 探头 |
| 拖动 | 拎起晃动反馈，松手回待机 |
| 提醒 | 到中央 → 文案 + 投喂 → 回位 |
| 启动坞打开 | 不抢菜单交互 |
| 大小 | 设置或托盘调节显示尺寸（默认偏小，约 60% 设计基准） |

### 4.3 健康提醒

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| F-RM-01 | 默认约 1 小时触发；间隔可在设置中调整 | P0 |
| F-RM-02 | 移到中央 → 动画 → 幽默文案 → 食物按钮 → 投喂 → 回原位 | P0 |
| F-RM-03 | 多条幽默提示，随机或轮换 | P0 |
| F-RM-04 | 托盘可暂停 / 恢复提醒 | P0 |
| F-RM-05 | 提醒中拖动不打断；拖完再处理待办提醒 | P1 |
| F-RM-06 | 自定义文案 | P2 |
| F-RM-07 | 智能提醒（使用时长） | P2 |

**文案示例**

1. 喂我之前，先喂一下你的健康：站起来走两步。  
2. 我已经快饿扁了，你的腰也快僵了。  
3. 主人，你不是我的坐骑，请不要骑着椅子不动。  
4. 再不起来，我就要怀疑你被椅子封印了。  

### 4.4 快捷启动

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| F-SC-01 | **单击**宠物展开启动坞 | P0 |
| F-SC-02 | 以宠物为锚点展开，风格统一、有展开感 | P0 |
| F-SC-03 | 按屏幕位置自适应，避免裁切 | P0 |
| F-SC-04 | 可添加 exe / 快捷方式 | P0 |
| F-SC-05 | 删除、排序；点击启动 | P0 |
| F-SC-06 | 删除只去应用内条目，不删磁盘文件 | P0 |
| F-SC-07 | 路径失效：保留条目 + 提示修复/删除 | P1 |
| F-SC-08 | 结构可扩展更多入口 | P1 |
| F-SC-09 | 全局快捷键启动 | P2 |

### 4.5 托盘与设置

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| F-TR-01 | 系统托盘图标 | P0 |
| F-TR-02 | 菜单：显示/隐藏宠物、**宠物变大/变小**、暂停/恢复提醒、设置、退出 | P0 |
| F-ST-01 | 设置：快捷方式管理 + 提醒相关 + **宠物大小** | P0 |
| F-ST-02 | 配置与列表本地持久化 | P0 |
| F-ST-03 | 宠物大小：步进调节（约 50%–150% 设计基准），写配置并即时/退出设置后生效 | P0 |

### 4.6 交互优先级（产品规则）

1. **用户拖动** &gt; 提醒打断（拖完再处理待办）。  
2. **提醒** &gt; 普通待机 / 自动扑近类互动。  
3. **启动坞打开时**以菜单为主，不抢扑近。  
4. 边缘隐藏时，点探头应能恢复。  

---

## 5. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 性能 | 典型待机 CPU &lt; 3%、内存 &lt; 200MB；点击/拖动/菜单应有即时反馈 |
| 打扰度 | 轻量存在；提醒可完成、可暂停；关设置 ≠ 退出应用 |
| 稳定 | 启动失败、路径失效、写配置失败不崩溃；给可读提示 |
| 隐私 | 无账号、无上传；仅访问用户主动添加的路径与本地配置；常规不需管理员 |
| 兼容 | Win10/11 x64；常见 DPI；多屏时宠物不得永久丢到不可见区 |

---

## 6. 产品约束

| 约束 | 说明 |
| --- | --- |
| 语言 | Rust |
| 平台 | 首期仅 Windows 桌面 |
| 形态 | 桌面悬浮宠物 + 本地配置（非 Web 套壳主路径） |

具体库与架构见 `tech.md`。

---

## 7. 验收标准（首期）

1. 启动后奶牛猫透明悬浮，可拖；重启恢复位置/配置。  
2. 待机为真眨眼；约 30s 内可见一次撒娇动作。  
3. 边缘半隐藏 + 探头，可恢复。  
4. 鼠标靠近有观察（飞扑不测）。  
5. 约 1 小时（测试可缩短）提醒：中央、文案、投喂、回位。  
6. 托盘：暂停/恢复提醒、显隐、**变大/变小**、设置、退出。  
7. 单击开启动坞；可增删排序启动；失效有提示。  
8. 设置或托盘可改宠物大小；重启后保持。  
9. 典型待机 CPU &lt; 3%、内存 &lt; 200MB。  
10. 无账号网络即可完成闭环。  

---

## 8. 开放问题

| ID | 问题 | 默认建议 |
| --- | --- | --- |
| Q1 | 提醒间隔是否可设置？ | 可，默认 60 分钟 |
| Q2 | 开机自启？ | 后期 / 默认关 |
| Q3 | 开菜单手势？ | 单击 |
| Q4 | 提醒「稍后」？ | P1；至少可暂停 |
| Q5 | 多屏拔掉宠物所在屏？ | 移回主屏安全区 |

---

## 9. 变更流程

1. 改本文并更新版本记录。  
2. 标明变更类型：范围 / 行为 / 非功能 / 约束。  
3. 同步 `tech.md`、`design.md`（必要时 `task.md`）。  
4. 再改代码。  
5. 仅视觉 → 可只改 design；仅实现重构且行为不变 → 可只改 tech。  

---

## 10. 版本记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v0.1 | 2026-07-31 | 初稿 |
| v0.2 | 2026-08-01 | 补全产品结构 |
| v0.3 | 2026-08-04 | 同步动作与里程碑表述 |
| **v0.4** | **2026-08-04** | **精简重组**：去掉实现细节；启动坞产品表述去「径向」绑定；飞扑明确后期 |
