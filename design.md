# 桌面快速访问互动宠物 UI 设计文档

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 项目名称 | 桌面快速访问互动宠物（PawDesk） |
| 文档类型 | UI / 交互设计文档 |
| 目标平台 | Windows 10/11，透明悬浮窗 |
| 渲染技术 | `wgpu`（宠物主窗口）+ 可选 `egui`（设置窗口） |
| 依据文档 | `prd.md` **v0.2**、`tech.md` v0.1 |
| 当前版本 | v0.2（2026-08-04 同步实现） |

本文档在 `prd.md` 定义的功能范围和 `tech.md` 的技术约束（透明悬浮窗、精灵帧动画、低 CPU/内存占用、快捷启动坞、状态机驱动）之内，给出具体的视觉与交互设计方案，并在第 12 章给出若干超出原始需求的体验建议。

### 1.1 实现阶段说明（与 task 对齐）

| 阶段 | 设计侧重点 |
| --- | --- |
| M0–M5 | 功能可用 + 启动坞 Apple 风初版 + 设置提醒分区 |
| **M6-A/B** | **交互正确性优先**于视觉炫技 |
| **M6-C** | UI 修复：间距、命中、按钮态、提醒卡可读性 |
| **M6-D 及以后** | **宠物动作与形象精修放最后**（飞扑、新 clip、重绘） |

当前实现要点：快捷启动为**宠物旁启动坞卡片**（非强制径向环）；菜单/设置走 CPU 自绘 + DPR 物理像素；勿在 M6 前期大改角色精灵管线。

---

## 2. 设计目标与原则

1. **轻量存在感**：宠物常驻桌面，但不能像弹窗一样打扰用户；视觉上要"小而萌"，交互上要"低打扰、高情感"。
2. **状态可读性优先**：`pet.state.rs` 中的每一个状态（Idle / Watching / Approaching / PlayingInteraction / Reminder / MenuOpen / Dragging / HiddenAtEdge）都必须有唯一且易辨识的视觉表现，用户无需思考就能看懂宠物在干什么。
3. **动作即反馈**：所有交互（点击、拖动、菜单展开、投喂）都必须在 100ms 内给出动效反馈，避免"点了没反应"的迟钝感。
4. **风格统一**：宠物、菜单、提醒气泡、设置面板共用同一套色板、圆角、描边和动效曲线，让人感觉是"同一只猫的世界"，而不是"猫 + 一个 Windows 系统弹窗"。
5. **渲染成本可控**：一切视觉方案必须能在 `Renderer` 的四个绘制原语（`draw_sprite` / `draw_text` / `draw_panel` / `draw_button`）内实现，不引入需要复杂 shader 或大纹理常驻的效果。

---

## 3. 视觉风格总纲

### 3.1 风格定位

- **画风**：日系治愈系二次元卡通风格，圆润造型 + 大眼睛 + 简化肢体，参考"托卡"、"波奇塔"一类小体积桌宠角色的比例（头身比约 1:1.2，Q 版）。
- **氛围关键词**：软萌、慵懒、偶尔傲娇、有点粘人。奶牛猫的黑白花纹用来做视觉锚点，方便用户一眼在桌面上找到它。
- **基调**：整体高明度、低饱和度的暖色调背景元素（菜单面板、提醒气泡），宠物本体保持高饱和黑白撞色以突出主体。

### 3.2 色板（Design Tokens）

| Token | 色值（示例） | 用途 |
| --- | --- | --- |
| `color.pet.fur.base` | `#FFFFFF` | 猫身体底色（白） |
| `color.pet.fur.spot` | `#2B2B2E` | 奶牛斑纹（黑） |
| `color.pet.nose` | `#FFB6C1` | 鼻子/肉垫粉 |
| `color.accent.primary` | `#FF9EC4` | 主强调色（食物按钮、选中态、气泡边框） |
| `color.accent.secondary` | `#8ED1D6` | 次强调色（菜单高亮、拖动反馈） |
| `color.surface.panel` | `#FFF8F2` @ 92% 透明 | 菜单/气泡/设置面板底色 |
| `color.surface.panelDark` | `#2A2730` @ 92% 透明 | 深色模式下面板底色 |
| `color.text.primary` | `#3A3540` | 面板正文文字 |
| `color.text.onAccent` | `#FFFFFF` | 强调色按钮上的文字 |
| `color.state.warning` | `#FFB020` | 快捷方式失效提示 |
| `color.state.danger` | `#FF5C5C` | 删除确认 |
| `color.shadow.soft` | `#000000` @ 12% | 面板投影，柔化边界 |

深浅两套主题都要覆盖，跟随 Windows 系统的浅色/深色模式自动切换（读取系统主题一次，运行期间不做实时跟随以节省开销，除非用户在设置里手动切换）。

### 3.3 圆角、间距与描边

- 圆角统一使用 4 档：`4 / 8 / 16 / 24`（px，96 DPI 基准）。宠物气泡、菜单圆形项使用 24；按钮用 8；小图标容器用 4。
- 间距基准单位 `8px`，面板内边距统一 `16px`，元素间距 `8px` 或 `12px`。
- 描边：面板与按钮使用 `1.5px` 半透明白色内描边（模拟玻璃拟态高光），避免在透明桌面背景上"糊"成一片。

### 3.4 字体与排版

- 中文字体优先使用系统自带"微软雅黑 / Segoe UI"，避免打包字体增加体积；如需更贴合萌系风格，可选打包一款圆体（如"站酷快乐体"授权版）仅用于提醒文案和菜单标题，正文仍用系统字体保证清晰度。
- 字号档位：提醒大字 `20px`、面板标题 `16px`、正文 `14px`、辅助说明 `12px`。
- 行高统一为字号的 1.4 倍。

### 3.5 动效曲线

统一使用两条缓动曲线，在渲染器里做成常量，供动画、移动、菜单展开复用：

- `ease.snappy`（cubic-bezier(0.34, 1.56, 0.64, 1)）：带轻微回弹，用于菜单展开、按钮按下反馈、投喂成功反馈。
- `ease.smooth`（cubic-bezier(0.22, 0.61, 0.36, 1)）：无回弹的平滑减速，用于宠物移动、边缘隐藏/探头、面板淡入淡出。

---

## 4. 宠物形象设计规范

### 4.1 尺寸与锚点

- 待机基础尺寸：**128×128px**（96×96 精灵内容 + 透明边距，便于扑跳等动作超出基础框而不裁切）。
- 提供 `@1x/@1.5x/@2x` 三套精灵图以适配不同系统缩放（100%/150%/200%），避免运行时缩放模糊。
- 锚点（`anchor`）固定在猫爪着地中心点，所有动作围绕该点对齐，保证切换动作时不会"跳位"。

### 4.2 状态 → 视觉映射表（与实现对齐 2026-08-04）

| 状态（PetState） | 视觉 / 资源 clip | 动帧要点 |
| --- | --- | --- |
| Idle base | idle_blink：坐姿呼吸 + 眨眼 | **默认日常**；视频抽帧循环 |
| Idle 定时动作 | idle_stretch / idle_cute / idle_tail_wag / idle_sleep | 约 **30s** 随机一次，one-shot 后回 base |
| Watching | idle_watch | 中距观察；不进 30s 池 |
| Approaching | approaching（**当前关闭飞扑**） | 后期专项；近距暂用 Watching |
| PlayingInteraction | playing_interaction | 落地互动约 1.5s → 回位待机 |
| Reminder（非投喂） | reminder_wave | 移向中央 / 展示阶段 |
| Reminder(Feeding) | reminder_feed | 点食物后的满足态 |
| MenuOpen | idle_blink | 坐姿，不抢菜单焦点 |
| Dragging | dragging | 拎起晃动 |
| HiddenAtEdge | edge_peek | 边缘探头 |

画布基准：**256x256** 精灵（逻辑窗 128，高 DPI 物理约 256 近 1:1）。完整规格见 tech.md 第 6.1.1 节。

### 4.3 待机规则（视觉层）

- **默认**：idle_blink 坐姿呼吸/眨眼，不连播大动作。
- **定时动作**：base 上约 **30s** 后随机 one-shot（伸懒腰 / 卖萌 / 轻摇 / 小睡），防短历史连播。
- **计时**：墙钟 30s（Watching 不重置）；飞扑 / 菜单 / 提醒 / 拖动期间不启动撒娇，结束后继续累计。
- **连贯性**：视频抽帧为主；呈现侧邻帧平滑采样；飞扑姿势绑定位移进度。
- **抠图**：仅品红背景；禁止黑键（伤黑毛）。

---

## 5. 主窗口与场景行为设计

### 5.1 窗口层级与命中区域

- 宠物窗口整体透明，只有宠物精灵的非透明像素区域接受鼠标事件（alpha-based hit testing），其余区域鼠标穿透，不遮挡桌面操作——这是本设计中最高优先级的可用性要求。
- 窗口默认置顶但不抢焦点（`WS_EX_NOACTIVATE` 语义），点击宠物不会把其他全屏应用切出焦点。

### 5.2 屏幕边缘行为的视觉细节

- 阈值区域建议：距屏幕边缘 **24px** 内触发隐藏判定。
- 隐藏比例：默认隐藏 60%身体，保留头部或爪子作为"探头"可点击区域（约 40×40px 命中框），避免用户误以为宠物消失。
- 探头状态下宠物耳朵/眼睛仍有低频待机动画（如眨眼），提示"我还在"。
- 恢复动画：鼠标进入探头区域 200ms 内触发"整只滑出"动效，使用 `ease.smooth`，时长 250ms。

### 5.3 鼠标互动的距离分级视觉反馈

建议将 `interaction.rs` 的距离检测细化为三档，每档对应不同视觉强度，而不是"检测到/未检测到"的二元状态：

| 距离档位 | 视觉反馈 |
| --- | --- |
| 远（>300px） | 正常待机，无反应 |
| 中（120–300px） | 切换为 `Watching`，头部/眼球跟随鼠标 |
| 近（<120px） | 触发 `Approaching` 飞扑（弧线 + 扑姿），到达后 `PlayingInteraction` |

这样鼠标越靠近，宠物"越有反应"，比单一阈值更自然，也方便后续做成可配置的灵敏度选项。

---

## 6. 健康提醒 UI 设计

提醒是本产品除快捷启动外最重要的情感触点，设计上要"有梗但不烦人"。

### 6.1 视觉流程

1. **进入提醒**：宠物当前位置原地播放"探出身体张望"过渡动作（0.3s），随后沿贝塞尔路径滑向屏幕工作区中心，移动时长约 600–800ms（`ease.smooth`），移动中宠物朝向面向屏幕中心。
2. **提醒主体（中央面板）**：
   - 一个圆角 24px 的浮动卡片（`color.surface.panel`），宠物立于卡片上方偏左，文案气泡在右侧展开。
   - 文案气泡为"漫画对话气泡"造型（圆角矩形 + 三角尾巴指向猫嘴），文字使用打字机效果逐字出现（约 30 字/秒），增强"猫在跟你说话"的感觉。
   - 卡片下方居中放置"食物按钮"（🐟/🍖图标 + 强调色 `color.accent.primary` 背景，圆形，直径 56px），按钮有待点击的呼吸缩放动效（scale 1.0↔1.05，1.2s 循环）提示可交互。
3. **投喂反馈**：点击食物按钮后，按钮播放"被吃掉"消失动效（缩小 + 粒子式的小面包屑飞溅——用简单的 3–5 个小 sprite 抛物线运动模拟，不使用真正粒子系统），宠物播放"满足眨眼 + 打嗝"动作，气泡文字切换为一句简短感谢/结果反馈（如"呼～活力恢复了！"）。
4. **返回**：0.5s 停留后，宠物沿原路径反向滑回出发位置，路径与去程保持视觉对称。

### 6.2 交互细节

- 提醒卡片右上角提供一个极小的"×"或"稍后提醒"文字按钮（12px 字号，弱化处理，避免用户过度依赖"随手关掉"而失去提醒意义，但必须提供，尊重用户当前操作）。
- 若提醒过程中用户开始拖动宠物或触发菜单，卡片立即淡出（150ms），状态机记录"提醒待处理"，拖动/菜单结束后自动重新触发提醒流程（与 `tech.md` 状态优先级设计一致）。
- 文案库建议按"关心程度"分级（轻松吐槽 / 直接关心 / 略带撒娇），随机挑选时避免连续两次同一分级，减少审美疲劳。

### 6.3 无障碍/低打扰选项（建议项，见第 12 章详述）

提醒卡片应支持"迷你模式"：不移动到屏幕中心，只在宠物头顶弹出一个小气泡+按钮，供专注工作时使用的用户选择（设置项）。

---

## 7. 快捷启动（Launcher）设计

> 本节为产品/体验规范，**不绑定**具体圆角数字或必须径向环。实现以可读、好用、贴合桌宠气质为准。

### 7.1 产品目标

用户单击小猫，用最少步骤启动常用软件：  
**一眼看懂 → 一键启动 → 需要时再管理**。

### 7.2 形态：宠物旁「启动坞」卡片

- **形态**：圆角浅暖色卡片（Launcher Dock），**小猫肖像在一侧**，**操作列表在另一侧**，而不是硬性 360° 环绕遮挡。
- **尺寸**：约 440×300 逻辑像素（可随 DPI 缩放），卡片半透明奶油底 + 顶部粉色点缀，与黑白猫形成对比。
- **展开方向**：根据屏幕空余自动贴左/贴右（避免出屏）；卡片锚定使小猫肖像大致落在原桌宠位置，减少跳动感。
- **动效**：约 250ms 淡入/位移动画（open_t），干脆不拖沓。

### 7.3 信息架构

| 区域 | 内容 | 交互 |
| --- | --- | --- |
| 肖像区 | 缩小的宠物立绘 +「点击关闭」 | 单击关闭启动坞 |
| 顶栏操作 | **＋ 添加** / **管理** / **暂停提醒** | 芯片按钮，主色区分功能 |
| 列表区 | 已启用快捷方式（最多约 5 条） | 整行可点；右侧「启动」胶囊强调 |
| 空状态 | 「还没有常用软件…」文案 | 引导点「＋ 添加」 |

固定入口职责：

1. **＋ 添加**：直接打开系统文件选择器（.exe / .lnk），添加后立刻出现在列表（最快路径）。
2. **管理**：进入「快捷启动管理」全页，负责排序、启停、删除、批量维护。
3. **暂停/恢复提醒**：一键切换健康提醒，不离开启动坞。

### 7.4 操作流

\\	ext
单击宠物
  → 打开启动坞
      → 点列表项 / 「启动」→ 启动程序并关闭坞
      → 点「＋ 添加」→ 系统器 → 写回配置 → 列表刷新
      → 点「管理」→ 管理页（见 §8）
      → 点「暂停提醒」→ 切换状态，坞可保持打开
      → 点肖像区或卡片外逻辑关闭区 → 关闭坞，宠物回原位
\
失效快捷方式：行内警告标记；点击进入管理页修复/删除，而不是无声失败。

### 7.5 视觉气质（自由发挥准则）

- **萌系桌宠 + 轻工具**：圆角卡片、粉/青强调色、柔阴影；避免系统灰窗。
- **可读优先**：完整中文标签；不用截断到乱码的两字块。
- **层次**：卡片阴影 → 底板 → 肖像 → 操作芯片 → 列表行。
- **透明桌面友好**：卡片本体不透明/高不透明，避免和壁纸糊成一片；窗外仍透明显示。

---

## 8. 快捷启动管理页

不再强制独立 egui 窗；首期可用自绘面板（与启动坞同一呈现管线），保证风格统一。

### 8.1 结构

- 标题「快捷启动管理」+ 简短说明。
- 列表：名称、有效性、启停、上移/下移/删除。
- 底部主按钮「＋ 添加快捷方式」（全宽、粉色）。
- 右上角关闭。

### 8.2 操作

| 操作 | 行为 |
| --- | --- |
| 添加 | 系统文件选择器 → 写入 config → 列表刷新 |
| 启停 | 禁用后不出现在启动坞列表 |
| 排序 | 上/下调整 sort_order，启动坞顺序同步 |
| 删除 | 仅删配置项，不删磁盘文件 |
| 关闭 | 回桌宠原位；若从启动坞进入，可视为结束一次管理会话 |

### 8.3 与托盘关系

托盘「打开设置」可直接进入本管理页（或未来完整设置）；启动坞是**高频路径**，管理页是**低频维护路径**。

## 9. 系统托盘设计

- 托盘图标：宠物头像的极简化图标（16×16 / 32×32 两档），保持与主形象一致的黑白花纹特征，确保在浅色/深色任务栏下都清晰可辨（建议描边 1px 保证深色任务栏下不糊边）。
- 右键菜单项：`显示/隐藏宠物`、`暂停提醒 / 恢复提醒`（根据当前状态切换文案）、`打开设置`、分隔线、`退出`。
- 菜单文案与图标风格尽量贴近系统原生右键菜单规范（因为这是 OS 原生控件，不强行做视觉定制，保证系统一致性和可访问性）。

---

## 10. 交互反馈与微动效清单

| 场景 | 反馈 |
| --- | --- |
| 点击宠物但未成功打开菜单（如正在移动中） | 宠物短促"眨眼+耳朵动一下"表示"收到但暂时不响应" |
| 拖动开始 | 阴影加深 + 轻微放大 3%，明确"已抓起" |
| 拖动结束松手 | 落地时轻微下压回弹（squash & stretch，1 帧压扁+2 帧回弹） |
| 快捷方式启动成功 | 菜单图标一闪（scale 1.0→1.2→1.0，120ms）后菜单自动收起 |
| 快捷方式启动失败 | 图标轻微左右抖动（shake，3 次，80ms/次）+ 错误 tooltip |
| 配置保存中 | 无可见 UI（后台防抖静默保存），仅在失败时才在托盘图标短暂加一个小红点角标 |

---

## 11. 响应式与多屏 / DPI 适配

- 所有尺寸以 96 DPI 为基准，运行时按系统 DPI 缩放系数统一缩放精灵、面板与命中区域，避免高 DPI 屏幕下菜单文字/图标过小。
- 多显示器场景下，径向菜单和提醒卡片的展开计算必须基于宠物**当前所在显示器**的工作区边界，而不是虚拟桌面整体边界，否则会出现菜单跨屏裁切的问题。
- 显示器分辨率或数量变化（拔插外接屏）时，若宠物所在工作区消失，按 tech.md 规则将其位置钳制回主显示器安全区域，并在下次出现时用一次轻量的"滑入"动效代替直接瞬移，降低突兀感。

---

## 12. 总体优化建议（超出 prd/tech 原始范围）

以下建议不改变技术架构，均可在现有状态机与渲染接口内实现，供评估是否纳入首期或后续迭代：

1. **好感度/心情的轻量化表达**：不做完整养成系统，但可以让"投喂及时率"轻微影响待机动画权重（长期不理会，`Sleep`/委屈类动作权重上升；经常互动，`ActingCute` 权重上升）。这能让 6 个待机动作显得"有记忆"，而不是纯随机播放，情感粘性更强，且实现成本仅是给权重表加一个输入变量。
2. **提醒的"专注模式感知"**：可选检测当前是否有全屏应用（如视频会议、游戏）在前台，若是则自动降级为静默/迷你提醒或延后触发，避免打断重要场景——这是对 prd 中"根据电脑使用时间智能提醒"的一个低成本前置版本。
3. **快捷菜单的键盘可达性**：为菜单项增加数字键（1–9）快速选中的隐藏支持，方便熟练用户不用精确点击小圆点，也为未来"全局快捷键"预留了交互习惯上的一致性。
4. **右键宠物直达最近使用的 3 个快捷方式**：区别于左键的完整径向菜单，右键弹出一个极简小竖条（最近使用/置顶的 3 项），覆盖"我知道我要开什么，别让我点两次"的高频场景。
5. **失效快捷方式的视觉降权而非隐藏**：菜单中不要直接隐藏失效项（用户会疑惑"我加的东西呢"），而是用第 7.3 节的警告角标保留展示，点击给出修复引导，兼顾透明度和可用性。
6. **深浅主题联动桌面壁纸亮度（可选、后期）**：Windows 可读取当前壁纸主导色估算明暗，让面板色板自动微调对比度，避免"白猫在纯白壁纸上失踪"的边界情况；首期若成本高可先只做"系统浅色/深色模式跟随"。
7. **设置面板增加"一键还原默认布局"**：位置、大小、透明度调乱后，提供单个按钮恢复出厂设置，降低用户因为误操作而产生的挫败感，实现成本很低（清空对应配置字段并触发默认值）。

---

## 13. 资源清单（供美术/动画对接）

| 类别 | 数量估计 | 说明 |
| --- | --- | --- |
| 待机动画精灵序列 | 6 套（含预留 1 套） | 每套 6–12 帧不等，见 4.2 |
| 互动/移动动画 | Watching 转头、Approaching 跑动、PlayingInteraction × 3 变体 | 共 5 套 |
| 提醒相关 | 移动帧复用 Approaching、提醒站姿+挥手 1 套、投喂满足反应 1 套 | 2 套新增 |
| 边缘探头 | 探头静止 + 眨眼循环 1 套 | 1 套新增 |
| 菜单图标 | 固定入口 3–5 个（软件管理/设置/暂停提醒/关于/退出预留），动态快捷方式图标取自 `.lnk`/`.exe` 自带图标 | 图标需统一裁切为圆形蒙版 |
| 托盘图标 | 16×16、32×32 各 1 套（浅/深任务栏各一版或用描边兼容两者） | 2–4 张 |
| UI 音效（可选） | 点击、菜单展开、投喂成功、错误提示 | 首期可不做，预留接口 |

---

## 14. 与 tech.md 的接口对齐说明

- 本文档所有视觉状态均一一对应 `tech.md` 第 6.1 节 `PetState` 枚举，不新增会破坏状态机的额外状态；第 12 章建议若被采纳，仅需扩展 `AppConfig`/`ReminderConfig` 字段和动画权重表，不改变状态机结构。
- 所有面板/按钮/文本的绘制均可通过 `tech.md` 第 4.1 节定义的 `Renderer` trait（`draw_sprite` / `draw_text` / `draw_panel` / `draw_button`）实现，无需扩展渲染接口。
- 动效时长与帧率建议已考虑 `tech.md` 8.3 节的性能策略（待机 12–24 FPS，菜单/移动状态临时提高刷新率），本文档中的具体毫秒数可作为该策略的默认参数输入。
