# PawDesk 视觉与交互设计文档

| 项目 | 内容 |
| --- | --- |
| 版本 | **v0.4** |
| 依据 | `prd.md` v0.4 · `tech.md` v0.3 |
| 效果图 | `docs/mockups/launcher-preview.*` · `launcher-pin-flip.*` |

---

## 阅读导航

| 想了解… | 看 |
| --- | --- |
| 设计原则 | [§1](#1-设计目标与原则) |
| 色板 / 字号 / 圆角 / 缓动 | [§2](#2-视觉系统) |
| 宠物形象与各状态长什么样 | [§3](#3-宠物形象与状态视觉) |
| 桌面拖动、边缘、鼠标距离 | [§4](#4-桌面存在与基础交互) |
| **启动坞（钉宠）** | [§5](#5-快捷启动坞-launcher) |
| 健康提醒 UI | [§6](#6-健康提醒-ui) |
| 设置与托盘 | [§7](#7-设置与系统托盘) |
| 动效清单 | [§8](#8-动效清单) |
| 资源与对接 | [§9](#9-资源与对接) |

**当前拍板（实现向）**

- 启动坞 = **钉宠锚点 + 半透明玻璃卡片**（非径向环、非系统 Acrylic）。  
- 菜单 / 设置 / 提醒叠层：CPU 自绘 + HiDPI 物理像素。  
- 形象与飞扑精修放后期（见 `task.md` M6-D）。

---

## 1. 设计目标与原则

1. **轻量存在**：常驻但不吵；小而萌，低打扰。  
2. **状态可读**：用户一眼知道在待机、观察、提醒还是开了菜单。  
3. **动作即反馈**：点击 / 拖动 / 开坞 / 投喂 ≤100ms 内有反应。  
4. **同一世界**：宠、坞、提醒、设置共用色板与圆角气质。  
5. **成本可控**：自绘面板 + 精灵帧；不做重 shader / 实时桌面模糊。

---

## 2. 视觉系统

### 2.1 风格

- 画风：日系治愈 Q 版，圆润、大眼；奶牛猫黑白斑作桌面锚点。  
- 氛围：软萌、慵懒、偶发傲娇。  
- 面板：高明度暖色半透明；宠物本体高对比黑白。

### 2.2 色板（Tokens）

| Token | 色值 | 用途 |
| --- | --- | --- |
| `pet.fur.base` | `#FFFFFF` | 身体白 |
| `pet.fur.spot` | `#2B2B2E` | 斑纹 |
| `pet.nose` | `#FFB6C1` | 鼻 / 肉垫 |
| `accent.primary` | `#FF9EC4` | 食物钮、列表高亮、强调 |
| `accent.secondary` | `#8ED1D6` | 次强调 |
| `accent.system` | `#007AFF` | 启动坞主按钮（可保留系统蓝） |
| `surface.panel` | `#FFF8F2` @ ~78–88% | 玻璃面板底 |
| `surface.row` | 浅暖灰半透明 | 列表行 |
| `text.primary` | `#3A3540` | 正文 |
| `text.secondary` | 灰 60–70% | 说明 |
| `text.onAccent` | `#FFFFFF` | 强调钮字 |
| `state.warning` | `#FFB020` / 深橙字 | 失效 |
| `state.danger` | `#FF5C5C` | 删除 |
| `shadow.soft` | 黑 8–16% | 多层软阴影 |

深色主题：面板 `#2A2730` @ 类似透明度（首期可读系统主题一次；运行中可不实时跟随）。

### 2.3 圆角 · 间距 · 描边

| 用途 | 值（96 DPI） |
| --- | --- |
| 大面板 / 坞 | 20–24px |
| 按钮 / 行 | 10–12px |
| 小芯片 | 8px |
| 间距基准 | 8px；内边距 16px |
| 玻璃描边 | 1px 半透明白内描边 + 顶部高光带 |

### 2.4 字体

- 系统：微软雅黑 / Segoe UI。  
- 档位：提醒 20 · 标题 16–17 · 正文 14–15 · 辅助 11–12。  
- 行高约 1.4×。

### 2.5 缓动

| 名 | 曲线 | 用途 |
| --- | --- | --- |
| `ease.snappy` | 微回弹 | 开坞、按下、投喂成功 |
| `ease.smooth` | 减速 | 移动、边缘、关坞淡出 |

实现：`src/render/easing.rs`。

---

## 3. 宠物形象与状态视觉

### 3.1 尺寸

- 窗逻辑基准：**128×128**（内容可小于框，留透明边）。  
- 精灵资源常见 **256×256** 帧，显示时适配。  
- 锚点：着地点附近，切换 clip 不跳脚。

### 3.2 状态 → 视觉

| 用户感知 | 状态 | 视觉要点 |
| --- | --- | --- |
| 日常待机 | Idle base | 坐姿呼吸 + 眨眼 |
| 撒娇 | Idle one-shot | 伸懒腰 / 卖萌 / 轻摇 / 小睡，可看清 |
| 在看你 | Watching | 抬头/注视循环 |
| 飞扑（后期） | Approaching + Playing | 扑近 + 落地互动 |
| 被拎起 | Dragging | 晃动 / 拎起感 |
| 躲边 | HiddenAtEdge | 半身 + 探头 |
| 催你起来 | Reminder | 中央、挥手/提醒姿 → 投喂姿 |
| 开了工具 | MenuOpen | 坐姿待机，不抢坞 |

资源目录名与映射见 `tech.md` §3.2。

### 3.3 待机节奏（体验）

- 默认安静眨眼。  
- 约 30s 来一次撒娇；鼠标在旁边观察时**不要饿死**这一定时感。  
- 撒娇动作忌「闪一下」——要够长。

---

## 4. 桌面存在与基础交互

### 4.1 窗口

- 真透明、无标题栏、轻置顶。  
- 透明像素不挡桌面；实体可点。  
- 关窗 = 隐藏或忽略；**退出只在托盘**。

### 4.2 拖动

- 按下移动超过阈值才算拖（避免和单击开坞冲突）。  
- 拖动有缩放/晃动反馈；松手回待机。  
- 拖动优先于提醒流程打断。

### 4.3 边缘

- 靠近边缘 → 部分藏入，留探头。  
- 点探头或靠近恢复。  
- **开启动坞前**须先完全露出来（避免锚点在屏外）。

### 4.4 鼠标距离（体验）

| 距离感 | 行为 |
| --- | --- |
| 远 | 不理会 |
| 中 / 近 | **观察** |
| 近 + 飞扑（后期） | 扑近互动 |

需迟滞，避免边界来回抽风。

---

## 5. 快捷启动坞（Launcher）

### 5.1 体验目标

```text
看见猫 → 点一下 → 工具从身边长出 → 一点启动 → 收起
```

原则：一眼认、一步启、不打断桌面、**宠物是锚不是装饰**。

### 5.2 形态

- **玻璃卡片** + 宠物钉在屏幕原位附近。  
- 无系统标题栏；半透明暖白 + 软阴影 + 内高光。  
- **假磨砂即可**，不要系统 Acrylic / 抓屏模糊。  
- 卡片逻辑尺寸约 **360×300**；整窗 = 宠矩形 ∪ 卡片。

参考：`docs/mockups/launcher-pin-flip.png`。

### 5.3 空间布局（钉宠）

优先级：

```text
1. Flip   右空间不够 → 卡翻到左（再考虑上/下）
2. Shift  仍裁切 → 只滑卡片，尽量不动宠
3. Size   极少：略缩卡
4. 整体微移  仅兜底
```

目标：四边四角 **整卡在工作区内**；宠不「整卡硬推」式瞬移。

### 5.4 结构

| 区域 | 内容 |
| --- | --- |
| 标题 | 「快捷启动」+ 短说明 |
| 主按钮 | 添加应用 |
| 次按钮 | 管理 · 暂停/恢复提醒 |
| 列表 | 常用应用行（图标圆点 + 名 + chevron） |
| 宠物区 | 钉死锚点；「轻点关闭」 |

空列表：引导去点「添加应用」。

### 5.5 列表与异常

| 状态 | 表现 |
| --- | --- |
| 默认行 | 浅底半透明 |
| Hover | 更亮 / 略强阴影 |
| 按下 | 略深或粉感填充 |
| 失效 | 警告底；`!`；文案「无法找到程序 · 点此修复」 |
| 点失效 | 进设置并 **高亮该行** |

### 5.6 开闭动画

| 阶段 | 参数 |
| --- | --- |
| 打开 | ~250ms snappy；opacity 0→1；scale 0.92→1（绕宠心） |
| 关闭 | ~180ms smooth；收回后缩回宠物窗 |
| 布局 | **开局锁定 placement**，中途不二次 flip |

业务：

- 启动成功 → 关坞动画。  
- 添加文件框 → 坞可保持。  
- 管理 → 进设置（可瞬时关坞，避免拖动画）。  
- 关坞后 **~280ms** 防连环重开。

### 5.7 桌宠反馈（可选增强）

| 事件 | 文案方向 |
| --- | --- |
| 启动成功 | 「收到，马上打开～」 |
| 启动失败 | 「这个应用好像搬家了…」 |

首期可用日志 + 失效行代替气泡。

---

## 6. 健康提醒 UI

### 6.1 流程视觉

```text
原位 → 移到屏幕中央 → 提醒姿 + 幽默文案
     → 食物按钮 → 投喂反馈 → 回原位待机
```

### 6.2 交互

- 文案幽默、可投喂完成；托盘可暂停。  
- 提醒中拖动：不丢状态，拖完再继续。  
- 食物按钮命中清晰；主色可用粉强调 `accent.primary`。

### 6.3 文案

使用 PRD 文案池；可轮换。字号提醒档 20px 级。

---

## 7. 设置与系统托盘

### 7.1 设置面板

- 与启动坞同一套玻璃 / 圆角 / 字号气质。  
- 分区：**健康提醒**（开关、间隔、暂停）+ **常用应用**列表。  
- 列表：启用、上移、下移、删除；底部全宽「添加应用」。  
- 关闭设置 ≠ 退出应用。  
- 从启动坞点失效项进入时：**高亮对应行**。

### 7.2 托盘

| 项 | 说明 |
| --- | --- |
| 图标 | 宠物头像感小图 |
| 菜单 | 显示/隐藏宠物、暂停/恢复提醒、打开设置、退出 |
| 退出 | 唯一正常退出路径之一 |

---

## 8. 动效清单

| 交互 | 反馈 | 曲线 |
| --- | --- | --- |
| 单击开坞 | 卡从宠侧 scale+fade 入 | snappy ~250ms |
| 关坞 | fade+scale 出再缩窗 | smooth ~180ms |
| 列表 hover | 行底变亮 | 即时 |
| 主按钮按下 | 蓝色加深 | 即时 |
| 拖动 | 轻微放大/晃 | — |
| 边缘 hide/show | 滑入滑出 | smooth |
| 提醒到位 | 文案与食物出现 | smooth / snappy |
| 投喂 | 短反馈再回位 | snappy |

---

## 9. 资源与对接

### 9.1 资源目录

```text
assets/pets/cow-cat/<clip>/*.png + meta.json
assets/tray/icon.png
```

抽帧与视频工具见 `tools/`；色键仅品红。

### 9.2 效果图

| 文件 | 内容 |
| --- | --- |
| `docs/mockups/launcher-preview.html` | 方案 A/B 对比 |
| `docs/mockups/launcher-pin-flip.html` | 钉宠 Flip/Shift 场景 |
| 对应 `.png` | 导出静帧 |

### 9.3 与 tech 的对应

| 设计概念 | 技术落点 |
| --- | --- |
| 钉宠 / Flip/Shift | `ui/launcher_place.rs` |
| 玻璃坞绘制 | `render/menu_ui.rs` |
| open_t 动画 | `pet` menu_open_t + compose |
| 状态视觉 | `pet` clip 映射 |
| 呈现 | `UpdateLayeredWindow` |

---

## 10. 版本记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v0.1 | 2026-08-01 | 初稿 |
| v0.2–0.3 | 2026-08-04 | 同步启动坞卡片、钉宠 |
| **v0.4** | **2026-08-04** | **按模块重排**；阅读导航；与 PRD v0.4 / 钉宠+玻璃实现对齐；去掉过时「径向环默认」表述 |
